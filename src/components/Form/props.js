import PropTypes from 'prop-types';

const propTypes = {
  radio: {
    label: PropTypes.string,
  },
};

const defaultProps = {
  radio: {
    label: '',
  },
};

export { propTypes, defaultProps };
