import React, { Fragment } from 'react';

import { Form } from '../components/Form';

const RegisterForm = () => {
  return (
    <Fragment>
      <Form></Form>
    </Fragment>
  );
};

export default RegisterForm;
